git add . (marca los archivos para subirlos al repo).

git commit -m "Mensaje explicación" (cada vez que subimos un archivo hay q dejar un mensaje)

git remote add origin https://github.com/ivanGonzalezGit/dsoo.git

git push -u origin main

(para cambiar la rama de la carpeta .git en mi máquina -> git checkout -b "nombre_de_la_rama")

(para actualizar los archivos de mi máquina -> git pull origin main), (main es el nombre de la rama)
